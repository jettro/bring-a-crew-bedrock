# Working with Amazon Bedrock (Agents)

## Start working with Amazon Bedrock

First install the required packages:

```bash
poetry add boto3
```

Then login using aws cli:

```bash
aws sso login
```

